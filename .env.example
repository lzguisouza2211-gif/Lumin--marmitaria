# Supabase (Settings > API no painel do projeto)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Connection string do Postgres (Settings > Database > Connection string > URI)
# usada pelo Drizzle (migrations e query direta)
DATABASE_URL=
